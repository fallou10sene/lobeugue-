<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gallass.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Kayedieune"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme"
        android:name=".Administrator">
        <activity
            android:name=".MainActivity1"
            android:label="Nouveau Produit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RedirectActivity"
            android:label="Liste des Produits">
        </activity>
        <activity android:name=".MainActivity"
            android:label="Enregistrer un Produit">
        </activity>

    </application>

</manifest>